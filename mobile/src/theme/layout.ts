/** Phone-first width; legacy uses 480px on small screens. */
export const MOBILE_WEB_MAX_WIDTH = 480;

/** Desktop web shell — room for 2-column match grid like /legacy. */
export const WEB_APP_MAX_WIDTH = 720;

/** Bottom tab bar height on Expo web (custom WebTabBar). */
export const WEB_TAB_BAR_HEIGHT = 68;
